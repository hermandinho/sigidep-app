{
  "name": "sigidep-app",
  "version": "0.0.0",
  "scripts": {
    "lint": "prettier --check packages",
    "format": "prettier --write packages"
  },
  "husky": {
    "hooks": {
      "pre-commit": "echo 'Pre committing ...' && prettier --write . && git add -A ."
    }
  },
  "private": true,
  "devDependencies": {
    "husky": "4.2.3",
    "prettier": "^2.4.1"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^2.5.1"
  }
}
